const Conversation = (props: chatProp) => {
  return (
    <div className={props.styleClass}>
      <h1>Here is a conversation display area of each chat</h1>
    </div>);
};

export default Conversation;